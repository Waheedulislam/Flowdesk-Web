import { Badge,type BadgeProps } from "@/components/ui/badge"; import type { ProjectStatus } from "@/lib/api/project.api"; export function ProjectStatusBadge({status}:{status:ProjectStatus}){const variants:Record<ProjectStatus,BadgeProps["variant"]>={PLANNING:"secondary",ACTIVE:"success",IN_PROGRESS:"info",ON_HOLD:"warning",COMPLETED:"info",ARCHIVED:"secondary"};return <Badge variant={variants[status]}>{status.replaceAll("_"," ")}</Badge>}
