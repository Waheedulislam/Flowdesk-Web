import { Card,CardContent,CardHeader,CardTitle } from "@/components/ui/card"; import { ProjectStatusBadge } from "@/components/projects/project-status-badge"; import type { ProjectRecord } from "@/lib/api/project.api"; export function ProjectTable({projects,onOpen}:{projects:ProjectRecord[];onOpen:(p:ProjectRecord)=>void}){return <Card><CardHeader><CardTitle>Projects</CardTitle></CardHeader><CardContent className="overflow-x-auto px-0 md:px-6"><table className="min-w-full text-sm"><thead className="bg-muted/50 text-left text-xs uppercase tracking-wide text-muted-foreground"><tr><th className="px-4 py-3">Project</th><th className="px-4 py-3">Status</th><th className="px-4 py-3">Updated</th></tr></thead><tbody>{projects.map(p=><tr key={p.id} className="cursor-pointer border-t border-border/70" onClick={()=>onOpen(p)}><td className="px-4 py-3"><p className="font-medium">{p.name}</p>{p.description&&<p className="max-w-md truncate text-xs text-muted-foreground">{p.description}</p>}</td><td className="px-4 py-3"><ProjectStatusBadge status={p.status}/></td><td className="px-4 py-3 text-muted-foreground">{new Date(p.updatedAt).toLocaleDateString()}</td></tr>)}</tbody></table></CardContent></Card>}
